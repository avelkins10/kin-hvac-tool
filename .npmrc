# Hoist all packages to make them discoverable by Vercel's bundler
# Fixes: Cannot find module '@prisma/client-runtime-utils'
shamefully-hoist=true
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*
