{
  "buildCommand": "prisma generate && (npx prisma migrate deploy || echo 'Migrations skipped - may already be applied') && next build",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
